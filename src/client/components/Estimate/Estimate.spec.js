// import React from 'react';
// import TestUtils from 'react-dom/test-utils';
// import Estimate from './Estimate';

// describe('Estimate', () => {
//     it('should display expected text', () => {
//         const medianPrice = 1234;
//         const dataCount = 12;
//         const component = TestUtils.renderIntoDocument(<Estimate medianPrice={medianPrice} dataCount={dataCount} />);

//         expect(component.getDOMNode().textContent).toEqual(`Your estimated monthly rental value is $${medianPrice}.00 based on ${dataCount} data points`);
//     });
// });
