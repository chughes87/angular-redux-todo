// const errorMessage = require('./errorMessage');

// describe('errorMessage reducer', () => {
//     describe('ERROR_TRIGGERED', () => {
//         const errorMsg = 'don\'t panic';
//         const action = { type: 'ERROR_TRIGGERED', errorMsg };
//         const result = errorMessage(null, action);

//         expect(result).toBe(errorMsg);
//     });

//     describe('ESTIMATE_UPDATING', () => {
//         const action = { type: 'ESTIMATE_UPDATING' };
//         const result = errorMessage(null, action);

//         expect(result).toBe(null);
//     });
// });
