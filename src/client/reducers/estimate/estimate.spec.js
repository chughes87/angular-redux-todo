// const estimate = require('./estimate');

// describe('estimate reducer', () => {
//     describe('ERROR_TRIGGERED', () => {
//         const data = {};
//         const action = { type: 'EESTIMATE_UPDATED', estimate: data };
//         const result = estimate(null, action);

//         expect(result).toBe(data);
//     });

//     describe('ESTIMATE_UPDATING', () => {
//         const action = { type: 'ESTIMATE_UPDATING' };
//         const result = estimate(null, action);

//         expect(result).toEqual({ isLoading: true });
//     });
// });
